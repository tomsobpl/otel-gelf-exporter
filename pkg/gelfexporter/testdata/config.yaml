gelfudp:
  endpoint: "localhost:12201"
gelfudp/permessage:
  endpoint: "localhost:12201"
  endpoint_refresh_strategy: "perMessage"
gelfudp/interval:
  endpoint: "localhost:12201"
  endpoint_refresh_strategy: "interval"
gelfudp/interval15:
  endpoint: "localhost:12201"
  endpoint_refresh_interval: 15
  endpoint_refresh_strategy: "interval"
